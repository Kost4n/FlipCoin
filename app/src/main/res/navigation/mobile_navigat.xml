<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools"
            android:id="@+id/mobile_navigation"
            app:startDestination="@+id/navigation_flip">

    <fragment
            android:id="@+id/navigation_records"
            android:name="com.yes.or.no.app.RecordsFragment"
            android:label="@string/title_records"
            tools:layout="@layout/fragment_records"/>

    <fragment
            android:id="@+id/navigation_flip"
            android:name="com.yes.or.no.app.FlipFragment"
            android:label="@string/title_flip"
            tools:layout="@layout/fragment_flip"/>
</navigation>